version: '2'
services:
  # summon:
  summon-builder:
    build: .
    image: summon-builder
    volumes:
      - ./output:/go/src/github.com/cyberark/summon/output
    environment:
      GOOS:
      GOARCH:
