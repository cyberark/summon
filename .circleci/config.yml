version: 2.0

jobs:
  build:
    machine: true
    steps:
      - checkout
      # Build
      - run: ./build.sh

  test:
    machine: true
    steps:
      - run:
          name: Unit tests
          command: ./test.sh

workflows:
  version: 2
  build-and-test:
    jobs:
      - build
      - test:
          requires:
            - build
